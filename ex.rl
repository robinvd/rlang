import Base.Stdlib

extern "stdio.h" String puts(String)

binary 1 ; y (a : x, b : y) b

Num main()
  x = 20;
  puts("test");
  return (plus(2, 2));
  if 1 then
    puts("lel")
  else
    puts("sound and safe")
  end;
  return(a)

String prrr(a : String, b : String)
  puts(concat(a,b));
  return("done")

